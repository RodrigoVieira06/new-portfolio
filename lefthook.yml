pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint
    test:
      run: pnpm test

commit-msg:
  commands:
    conventional-commit:
      run: "pnpm exec -- commitlint --edit {1}"
